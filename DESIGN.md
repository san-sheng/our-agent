# 我们的 Agent —— 设计文档

> 版本：v0.1（草案）
> 维护者：少爷 + 奶琪
> 定位：**学习地图 + 架构蓝图**。不是生产系统的规格书，是「我们为什么要这样设计、每一步在学什么」的记录。
> 状态：随学习迭代，每完成一个里程碑回填一次。

---

## 1. 这份文档是什么

这份文档回答三个问题：

1. **我们要造什么** —— 一个不依赖 Hermes、从零写的独立 Agent
2. **我们怎么学** —— 从最基础开始，一个概念一个概念地实现，每块代码对应书里的一个原理
3. **我们走到哪了** —— 里程碑路线图，每完成一步就标记

核心参考书：《深入理解 AI Agent：设计原理与工程实践》（李博杰 著），书稿在本地 `~/wiki/raw/ai-agent-book/book/`。
配套代码仓库：https://github.com/bojieli/ai-agent-book

**为什么不是直接用现成框架？**
因为目标是学习。用 Hermes、LangChain 是在「消费」设计决策；自己写，才需要面对每一个取舍。我们有两年深度定制 Hermes 的经验（skill-router、Mnemosyne、hooks），这些经验现在反过来变成我们的对照系——书里的原则，很多我们已经在 Hermes 上实践过了，现在验证一遍「为什么」。

---

## 2. 核心公式（全书的地基）

> **Agent = LLM + 上下文 + 工具**
> 直觉版：**大脑 + 眼睛 + 手脚**

| 组件 | 直觉 | 作用 | 对应 RL 术语 |
|------|------|------|-------------|
| LLM | 大脑 | 理解意图、思考规划、做出判断 | Policy（策略） |
| 上下文 | 眼睛 | Agent 在每个决策点能看到的全部信息：环境、对话历史、工具定义 | Observation Space（观察空间） |
| 工具 | 手脚 | Agent 能做的所有事情 | Action Space（动作空间） |

**ReAct 循环**（思考→行动→观察）是 Agent 的基本运转方式：

```
模型思考 → 决定要做什么（或直接回答） → 框架执行工具 → 结果回填给模型 → 再思考……
```

直到模型不再调用工具、输出最终回答，循环终止。

---

## 3. 目标与非目标

### 目标
- 一个能跑起来的、我们自己写的、**能解释每一行**的 Agent
- 理解每个核心组件的设计取舍：为什么上下文要这样组织、工具描述怎么写、错误怎么恢复
- 每完成一个里程碑，写一篇学习笔记，沉淀「这块代码在实践什么原理」

### 非目标（初期明确不做）
- ❌ 功能上超越 Hermes —— 它是我们的对照参考，不是竞争对手
- ❌ 生产级健壮性 —— 初期只要本地能跑、可复现
- ❌ 多模态 / 分布式 / 多 Agent —— 放到路线图后期
- ❌ 界面花哨 —— 先 CLI，跑通了再说

---

## 4. 架构总览

### 4.1 目录结构（目标形态）

```
our-agent/
├── DESIGN.md          # 本文档
├── NOTES/             # 每里程碑的学习笔记（M1-notes.md ...）
├── agent/
│   ├── loop.py        # ReAct 主循环 + 多轮会话 + 状态栏（M1；M2 加会话历史/缓存统计/状态栏）
│   ├── context.py     # 上下文组装与管理（M1 起步，M2 深化）
│   └── state.py       # 会话状态（M2）
├── llm/
│   └── client.py      # LLM 客户端：OpenAI 兼容封装 + 重试 + usage 度量（M1；M2 加缓存尺子）
├── tools/
│   ├── registry.py    # 工具注册表：名字→函数、schema 生成（M1）
│   ├── base.py        # Tool 抽象（M1）
│   └── builtin/       # 内置工具（M1: read_file/write_file/run_command）
├── memory/            # 记忆系统（M3 引入）
├── cli.py             # 命令行入口（M1）
└── tests/             # 验证脚本（每个里程碑同步补）
```

### 4.2 数据流（一次任务怎么走）

```
你输入任务
   ↓
[cli.py] 收到输入，交给 agent.loop
   ↓
[context.py] 组装消息：system（不变）+ history + 新输入（追加到尾部）
   （M2 第 1 步已实现：history 由 agent/loop.py 的 self._history 提供，
    context.py 独立模块留待 M2 后续提取）
   （M2 第 2 步已实现：每轮请求末尾注入状态栏——时间戳/工具计数/失败计数/
     进度（剩余预算+临近警告）/TODO，书 Ch2 §2.6「提炼层」；只进请求不进历史；
     2026-08-25 修订去虚：失败统计 + 剩余预算替换无信息量格子；
     熔断时写 [UNFINISHED] 摘要进 history）
   （M2 第 3 步已实现：SYSTEM_PROMPT 四段结构化（身份/SOP/关键规则/安全边界，
     书 Ch2 §提示工程）+ 提示注入防御——工具结果回填前用 XML 来源标记包裹
     （<tool_result tool="...">，书 §提示注入 防御策略一），安全边界明确外部
     内容不可信、只遵循用户直接指令、不泄露系统提示词；先判断失败（原始
     result）后包裹（回填），失败统计不被破坏）
   ↓
[llm/client.py] 发给模型 → 返回文本 或 tool_calls[]
   ↓ 如果是 tool_calls
[tools/registry.py] 按名字找到工具函数 → 执行 → 结果作为 tool 消息回填
   ↓
回到「组装消息」继续循环（ReAct），直到模型输出最终回答
   ↓
[cli.py] 打印回答
```

---

## 5. 里程碑路线（学习顺序）

> 顺序对应书的结构：**构建（Ch1-5）→ 评估与进化（Ch6-8）→ 协作（Ch10）**。
> Ch7（模型后训练）了解性阅读，不做实验；Ch9（多模态）选读，暂不纳入。

| 里程碑 | 主题 | 书章节 | 交付物 | 验证方式 |
|--------|------|--------|--------|---------|
| **M1** | 最小 ReAct 闭环 | Ch1 + Ch4 工具入门 | LLM 客户端 + 3 工具 + 主循环 + CLI | 端到端 demo 跑通 |
| **M2** | 上下文工程 | Ch2 | KV Cache 友好组织、状态栏、上下文管理 | 观察 token 用量 / 注入测试 |
| **M3** | 记忆与知识库 | Ch3 | 会话记忆 → 长期记忆 → 简单 RAG | 跨会话召回测试 |
| **M4** | Coding Agent 深化 | Ch5 + Ch4 安全 | 七核心工具、编辑方案、错误恢复 | 代码任务实测 |
| **M5** | 评估 | Ch6 | 小型 eval 集 + LLM-as-Judge | 改动前后分数对比 |
| **M6** | 持续进化 | Ch8 | 从运行轨迹提取学习信号 | 反馈闭环 demo |
| **M7** | 多 Agent 协作 | Ch10 | 双 Agent 分工 demo | 协作任务实测 |

每个里程碑的标准流程（学习方法论）：

```
1. 读对应章节（可只读相关小节）
2. 我讲清楚原理 + 设计取舍 → 你提问
3. 我写代码（注释写「为什么」，不是写「是什么」）
4. 一起验证（跑 demo / 测试）
5. 写 NOTES/MX-notes.md 学习笔记
6. 回填本文档的进度
```

---

## 6. M1 详解：最小 ReAct 闭环（下一步就做这个）

### 6.1 学习点（写完 M1 你应当能回答）

- Agent 核心公式三件套分别对应代码里的哪个模块
- ReAct 循环为什么是「循环」：工具结果为什么必须回填给模型
- 上下文五组件（system / tools / user / assistant / tool）在消息列表里长什么样
- 工具描述为什么重要：模型靠 description 决定「什么时候用这个工具」
- 错误恢复：API 调用失败怎么办（书 Ch1：指数退避 + 抖动）

### 6.2 范围（刻意最小化）

**做：**
- `llm/client.py` —— OpenAI 兼容 SDK 封装（DeepSeek API），带重试
- `tools/base.py` + `registry.py` + 3 个内置工具（read_file / write_file / run_command）
- `agent/loop.py` —— ReAct 主循环，`max_iterations` 熔断
- `cli.py` —— 简单的命令行 REPL
- 一个端到端 demo 任务

**不做（M2+ 再做）：**
- ❌ Skills / 按需加载
- ❌ 记忆（M3）
- ❌ 上下文压缩（M2）
- ❌ 并行工具调用
- ❌ 流式输出
- ❌ 安全沙盒（M4 深化）

### 6.3 端到端 demo（M1 的验收标准）

输入一个自然语言任务，例如：

> 读 README.md，统计有多少行，把结果写到 stats.txt

预期行为：Agent 依次调用 read_file → run_command（或直接推理）→ write_file，最后用一句话汇报结果。
验证标准：stats.txt 内容与真实行数一致，且**再跑一次结果相同**（可复现）。

> ✅ 已验收（2026-08-23）：项目无 README.md，demo 改用 DESIGN.md。
> 任务「读 DESIGN.md，统计它有多少行，把结果写到 stats.txt」跑两次均写 223，
> 与 `wc -l DESIGN.md` 一致（两次一致 = 可复现）。

### 6.4 技术选型（M1）

| 项 | 选择 | 理由 |
|----|------|------|
| 语言 | Python 3.11 | 全生态最成熟，书配套代码同款 |
| 环境 | venv（PEP 668 系统） | 不污染系统 |
| LLM SDK | `openai` | DeepSeek 是 OpenAI 兼容协议；把精力留给 Agent 逻辑而非 HTTP 管道 |
| 模型 | deepseek-v4-flash | 本地 Hermes 同款，便宜够用 |
| 依赖 | 尽量少，初期只加 openai | 依赖越少，越看得清自己在写什么 |

---

## 7. 工程纪律（我们俩的约定）

1. **证明契约**：每步任务必须有「怎么验证完成了」的方式，不写「我看看」。
2. **每步验证**：改完代码必须跑通再进入下一步；无法验证 = 没完成。
3. **注释写为什么**：学习项目，代码注释解释设计取舍，不解释语法。
4. **安全边界**：工具执行要小心——初期 run_command 只在本机、只跑只读/安全命令；涉及删除、写系统目录的操作先问少爷。
5. **回填文档**：每个里程碑完成后，更新本文档（进度标记 + 学习笔记链接）。
6. **改动先备份**：任何涉及已有代码的修改，先确认能回滚。

---

## 8. 参考材料

| 材料 | 位置 | 用途 |
|------|------|------|
| 书稿全文 | `~/wiki/raw/ai-agent-book/book/`（ch1-ch10 + introduction + afterword） | 原理来源 |
| 书配套代码 | `~/wiki/raw/ai-agent-book/chapter*/` | 实验参考 |
| agent-design-from-book skill | `~/.hermes/skills/software-development/agent-design-from-book/` | 书的核心原则速查 |
| Hermes 源码 | `~/.hermes/hermes-agent/` | 对照参考（看成熟实现怎么做） |
| Codex 源码 | `~/codex/` | skill 选择器参考（M2 用） |

---

## 9. 开放问题（等少爷拍板）

- [ ] **项目命名**：先叫 our-agent，要不要起个正式名字？
- [x] **DeepSeek API key**：已解决（2026-08-02）——key 存项目根 `config.toml`（已 gitignore），`llm/client.py` 用 `tomllib`（Python 3.11 标准库）读取；优先级：显式传参 > config.toml，缺失抛 ValueError
- [x] **学习节奏**：M1 拆成小步走（客户端 → 工具 → 循环 → CLI）——第 1 步 LLM 客户端已完成（2026-08-03，见 `NOTES/M1-notes.md`），第 2 步工具系统已完成（2026-08-03，见 `NOTES/M1-step2-tools.md`），第 3 步 ReAct 主循环已完成（2026-08-15，见 `NOTES/M1-step3-loop.md`），端到端 demo 验收通过（2026-08-23，见 §6.3）。M2 拆小步（多轮对话 → 状态栏 → 提示注入 → Skills），第 1 步多轮会话 + 缓存度量已完成（2026-08-23，见 `NOTES/M2-step1-context.md`），第 2 步状态栏 + 熔断摘要已完成（2026-08-25，见 `NOTES/M2-step2-statusbar.md`），第 3 步 system prompt 结构化 + 提示注入防御已完成（2026-08-25，见 `NOTES/M2-step3-prompt-injection.md`）
- [ ] **界面**：M1 先用 CLI。要不要一开始就预留 Telegram 接口的位置（只留设计位，不实现）？
- [x] **熔断轨迹处理**：已拍板（2026-08-24）且已实现（2026-08-25，M2 第 2 步）——熔断时生成 `[UNFINISHED]` 摘要（任务目标/已完成/下一步/失败点，复用状态栏格式）写入 history，不死记原始轨迹；见 `NOTES/M2-step2-statusbar.md`
- [ ] **git**：要不要建本地 git 仓库管理？建的话用本地分支（我们 Hermes 的经验：自己用的代码别推到官方）

---

## 进度

- [x] v0.1 设计文档（2026-07-31）
- [x] M1 最小 ReAct 闭环（2026-08-23 验收通过：端到端 demo「读 DESIGN.md 统计行数写 stats.txt」两次跑结果一致，223 行）
- [ ] M2 上下文工程（进行中：第 1 步多轮会话 + 缓存度量完成 2026-08-23；第 2 步状态栏 + 熔断摘要完成 2026-08-25；第 3 步 system prompt 结构化 + 提示注入防御完成 2026-08-25；剩第 4 步 Skills 按需加载）
- [ ] M3 记忆与知识库
- [ ] M4 Coding Agent 深化
- [ ] M5 评估
- [ ] M6 持续进化
- [ ] M7 多 Agent 协作
